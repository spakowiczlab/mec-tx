% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tx_duration.R
\name{tx_duration}
\alias{tx_duration}
\title{Treatment Duration Analysis by Grouping Variable}
\usage{
tx_duration(
  timeline,
  meta,
  group_var,
  sample_col = "sample",
  type_col = "type",
  start_col = "start_year",
  end_col = "end_year",
  duration_unit = "months",
  exclude_types = NULL,
  min_n = 3L,
  plot = TRUE,
  plot_type = "box",
  title = NULL,
  palette = NULL
)
}
\arguments{
\item{timeline}{data.frame --- output of \code{tx_intervals()}.
Must contain columns for sample, type, start_year, end_year.}

\item{meta}{data.frame --- patient-level metadata (e.g., Cluster_surv).
Must contain \code{sample_col} and \code{group_var}.}

\item{group_var}{Character --- column name in \code{meta} for the grouping
variable (e.g., "CAlevel").}

\item{sample_col}{Character --- patient ID column (default "sample").}

\item{type_col}{Character --- treatment type column (default "type").}

\item{start_col}{Character --- interval start column (default "start_year").}

\item{end_col}{Character --- interval end column (default "end_year").}

\item{duration_unit}{Character --- "months" (default) or "years". Controls
the unit in output tables and plot axis labels.}

\item{exclude_types}{Character vector --- treatment types to exclude from
analysis (e.g., \code{c("Others")}). Default NULL.}

\item{min_n}{Integer --- minimum patients per group -- type to run a
test (default 3). Types with fewer are flagged but kept.}

\item{plot}{Logical --- produce a plot? (default TRUE)}

\item{plot_type}{Character --- "box" (default) or "violin".}

\item{title}{Character --- plot title. NULL for auto-generated.}

\item{palette}{Named character vector --- colours keyed by group levels.
NULL for automatic palette.}
}
\value{
A named list:
\describe{
  \item{duration_per_type}{data.frame: sample, type, group_var, duration
        (in \code{duration_unit})}
  \item{duration_total}{data.frame: sample, group_var, duration_total}
  \item{summary_table}{data.frame: type, group, n, mean, median, q25, q75,
        p_value (Wilcoxon)}
  \item{plot}{ggplot object (or NULL if \code{plot = FALSE})}
}
}
\description{
Computes per-patient treatment duration (calendar time) broken down by
treatment type and as a merged total. Compares groups using Wilcoxon
rank-sum tests and produces box/violin plots.
}
\examples{
\dontrun{
res <- tx_duration(
  timeline  = timeline_long_intv,
  meta      = Cluster_surv,
  group_var = "CAlevel"
)
res$summary_table
res$plot
}

}
